# Figure Provenance

The images in [`recorded/`](./recorded/) are unchanged copies of the six figures in the legacy `Assignment1/images/` folder.

Known caveats:

- `eda_summary.png` says row-level data was unavailable, but `data/train.csv` is now included.
- `model_metrics_comparison.png` shows `0.148` as random-forest validation RMSLE, while the transcript records `0.152` for validation and `0.148` for cross-validation.
- The figures are preserved historical summaries, not proof that the current notebook was executed.

Regenerate authoritative final figures from the same run used to produce the final result tables.
